local syncmap = require "src.imports.syncer.syncmap.shared"
local syncnode = require "src.imports.syncer.syncnode.shared"

local syncer = {}

syncer.syncmap = syncmap
syncer.syncnode = syncnode

return syncer
